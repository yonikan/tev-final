import { RestrictionContainerDirective } from './restriction-container.directive';

describe('RestrictionContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictionContainerDirective();
    expect(directive).toBeTruthy();
  });
});
