import { Routes } from '@angular/router';
import { TeamConfigurationComponent } from './team-configuration.component';


export const routes: Routes = [
    { path: '', component: TeamConfigurationComponent }
];